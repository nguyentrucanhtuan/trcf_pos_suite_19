<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Expense List View -->
    <record id="view_trcf_expense_list" model="ir.ui.view">
        <field name="name">trcf.expense.list</field>
        <field name="model">trcf.expense</field>
        <field name="arch" type="xml">
            <list string="Expenses" decoration-muted="state == 'paid'" decoration-info="state == 'draft'">
                <field name="trcf_reference"/>
                <field name="name"/>
                <field name="trcf_category_id"/>
                <field name="trcf_amount" sum="Total" widget="monetary"/>
                <field name="trcf_payment_method_id"/>
                <field name="trcf_payment_date"/>
                <field name="employee_id" widget="many2one_avatar_employee"/>
                <field name="state" widget="badge" decoration-success="state == 'paid'" decoration-info="state == 'draft'" decoration-warning="state == 'approved'"/>
                <field name="company_id" groups="base.group_multi_company"/>
            </list>
        </field>
    </record>

    <!-- Expense Form View -->
    <record id="view_trcf_expense_form" model="ir.ui.view">
        <field name="name">trcf.expense.form</field>
        <field name="model">trcf.expense</field>
        <field name="arch" type="xml">
            <form string="Expense">
                <header>
                    <button name="action_approve" string="Approve" type="object" 
                            class="oe_highlight" invisible="state != 'draft'"/>
                    <button name="action_mark_paid" string="Mark as Paid" type="object" 
                            class="oe_highlight" invisible="state != 'approved'"/>
                    <button name="action_reset_to_approved" string="Reset to Approved" type="object" 
                            invisible="state != 'paid'" groups="base.group_system"/>
                    <button name="action_reset_to_draft" string="Reset to Draft" type="object" 
                            invisible="state == 'draft'" groups="base.group_system"/>
                    <field name="state" widget="statusbar" statusbar_visible="draft,approved,paid"/>
                </header>
                <sheet>
                    <div class="oe_title">
                        <h1>
                            <field name="trcf_reference" readonly="1"/>
                        </h1>
                    </div>
                    <group>
                        <group>
                            <field name="name" placeholder="e.g., Office supplies purchase"/>
                            <field name="trcf_category_id" options="{'no_create': True, 'no_open': True}"/>
                            <field name="trcf_amount" widget="monetary"/>
                            <field name="employee_id" readonly="1"/>
                        </group>
                        <group>
                            <field name="trcf_payment_method_id" options="{'no_create': True}"/>
                            <field name="trcf_payment_date"/>
                            <field name="company_id" groups="base.group_multi_company" options="{'no_create': True}"/>
                        </group>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Expense Search View -->
    <record id="view_trcf_expense_search" model="ir.ui.view">
        <field name="name">trcf.expense.search</field>
        <field name="model">trcf.expense</field>
        <field name="arch" type="xml">
            <search string="Expenses">
                <field name="name"/>
                <field name="trcf_reference"/>
                <field name="trcf_category_id"/>
                <field name="employee_id"/>
                <separator/>
                <filter string="Nháp" name="draft" domain="[('state', '=', 'draft')]"/>
                <filter string="Đã duyệt" name="approved" domain="[('state', '=', 'approved')]"/>
                <filter string="Đã thanh toán" name="paid" domain="[('state', '=', 'paid')]"/>
                <separator/>
                <filter string="My Expenses" name="my_expenses" domain="[('employee_id.user_id', '=', uid)]"/>
                <separator/>
                <filter string="Category" name="category" context="{'group_by': 'trcf_category_id'}"/>
                <filter string="Payment Method" name="payment_method" context="{'group_by': 'trcf_payment_method_id'}"/>
                <filter string="State" name="state_group" context="{'group_by': 'state'}"/>
                <filter string="Employee" name="employee" context="{'group_by': 'employee_id'}"/>
            </search>
        </field>
    </record>

    <!-- Expense Kanban View -->
    <record id="view_trcf_expense_kanban" model="ir.ui.view">
        <field name="name">trcf.expense.kanban</field>
        <field name="model">trcf.expense</field>
        <field name="arch" type="xml">
            <kanban class="o_kanban_mobile" default_group_by="state">
                <field name="trcf_reference"/>
                <field name="name"/>
                <field name="trcf_category_id"/>
                <field name="trcf_amount"/>
                <field name="state"/>
                <templates>
                    <t t-name="kanban-box">
                        <div class="oe_kanban_global_click">
                            <div class="o_kanban_record_top">
                                <div class="o_kanban_record_headings">
                                    <strong class="o_kanban_record_title"><field name="trcf_reference"/></strong><br/>
                                    <span class="o_kanban_record_subtitle"><field name="name"/></span>
                                </div>
                                <span class="float-end badge rounded-pill" t-attf-class="#{record.state.raw_value === 'paid' ? 'text-bg-success' : 'text-bg-info'}">
                                    <field name="state"/>
                                </span>
                            </div>
                            <div class="o_kanban_record_body">
                                <field name="trcf_category_id"/>
                            </div>
                            <div class="o_kanban_record_bottom">
                                <div class="oe_kanban_bottom_left">
                                    <strong><field name="trcf_amount" widget="monetary"/></strong>
                                </div>
                                <div class="oe_kanban_bottom_right">
                                    <field name="employee_id" widget="many2one_avatar_employee"/>
                                </div>
                            </div>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

    <!-- Expense Action -->
    <record id="action_trcf_expense" model="ir.actions.act_window">
        <field name="name">Expenses</field>
        <field name="res_model">trcf.expense</field>
        <field name="view_mode">list,kanban,form</field>
        <field name="search_view_id" ref="view_trcf_expense_search"/>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create your first expense
            </p>
            <p>
                Track and manage all your business expenses in one place.
            </p>
        </field>
    </record>
</odoo>
