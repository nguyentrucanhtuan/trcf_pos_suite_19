<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">
    <t t-name="trcf_kitchen_screen.KitchenDashboardTemplate">
        <!-- Tailwind CDN -->
        <script src="https://cdn.tailwindcss.com?plugins=forms,container-queries"></script>
        <link href="https://fonts.googleapis.com/css2?family=Work+Sans:wght@400;500;700&amp;display=swap" rel="stylesheet" />
        <link href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:wght,FILL@100..700,0..1&amp;display=swap" rel="stylesheet" />
        
        <!-- Tailwind Config -->
        <script>
            if (typeof tailwind !== 'undefined') {
                tailwind.config = {
                    darkMode: "class",
                    theme: {
                        extend: {
                            colors: {
                                "primary": "#28a745",
                                "primary-light": "#e9f6ec",
                                "background-light": "#f8f9fa",
                                "background-dark": "#102218",
                                "text-primary": "#1f2937",
                                "text-secondary": "#6b7280",
                                "border-color": "#e5e7eb",
                                "urgent": "#dc3545",
                                "urgent-light": "#fdebec",
                                "new": "#ffc107",
                                "new-light": "#fff8e6",
                            },
                            fontFamily: {
                                "display": ["Work Sans", "Noto Sans", "sans-serif"]
                            },
                        },
                    },
                }
            }
        </script>

        <!-- Custom Styles -->
        <style>
            .material-symbols-outlined {
                font-variation-settings: 'FILL' 0, 'wght' 400, 'GRAD' 0, 'opsz' 24;
            }
            .kitchen-body {
                font-family: 'Work Sans', 'Noto Sans', sans-serif;
                background-color: #f8f9fa;
                color: #1f2937;
                min-height: 100vh;
                height: 100vh;
                overflow-y: auto;
                -webkit-overflow-scrolling: touch;
            }
            .line-completed .item-content {
                text-decoration: line-through;
                color: #6b7280;
            }
            
            /* Mobile scrolling - only scroll the main container */
            html, body {
                overflow: auto;
                -webkit-overflow-scrolling: touch;
            }
        </style>

        <div class="kitchen-body overflow-auto">
            <div class="relative flex w-full flex-col min-h-full pb-[env(safe-area-inset-bottom)]">
                <!-- Header -->
                <header class="sticky top-0 z-10 border-b border-gray-200 bg-gray-50/80 backdrop-blur-sm">
                    <div class="flex items-center justify-between p-4">
                        <div class="flex h-12 w-12 shrink-0 items-center justify-start">
                            <span class="text-3xl">🍳</span>
                        </div>
                        <h1 class="flex-1 text-center text-2xl font-bold leading-tight tracking-tight text-gray-800">
                            <t t-esc="state.screen_name"/>
                        </h1>
                        <div class="flex items-center justify-end gap-4">
                            <!-- Toggle công thức -->
                            <div class="flex items-center gap-2" t-on-click="() => this.toggleRecipe()">
                                <span class="cursor-pointer text-base font-medium text-gray-600">Công thức</span>
                                <div class="relative cursor-pointer">
                                    <div t-att-class="'h-6 w-11 rounded-full transition-colors ' + (state.showRecipe ? 'bg-green-500' : 'bg-gray-300')"></div>
                                    <div t-att-class="'absolute top-0.5 h-5 w-5 rounded-full bg-white shadow transition-all ' + (state.showRecipe ? 'left-[22px]' : 'left-0.5')"></div>
                                </div>
                            </div>
                            <!-- Toggle tổng hợp -->
                            <div class="flex items-center gap-2" t-on-click="() => this.toggleSummary()">
                                <span class="cursor-pointer text-base font-medium text-gray-600">Tổng hợp</span>
                                <div class="relative cursor-pointer">
                                    <div t-att-class="'h-6 w-11 rounded-full transition-colors ' + (state.showSummary ? 'bg-orange-500' : 'bg-gray-300')"></div>
                                    <div t-att-class="'absolute top-0.5 h-5 w-5 rounded-full bg-white shadow transition-all ' + (state.showSummary ? 'left-[22px]' : 'left-0.5')"></div>
                                </div>
                            </div>
                            <span class="text-base font-medium text-gray-500">
                                <t t-esc="state.draft_count"/> đơn
                            </span>
                        </div>
                    </div>
                </header>

                <!-- Main Content -->
                <main class="w-full p-2 flex-1">
                    <!-- Orders Grid (Responsive: <=1024px: 2 cols, >1024px: 3 cols) -->
                    <div class="grid grid-cols-2 gap-4 lg:grid-cols-3">
                        <t t-foreach="getOrdersByStatus('draft')" t-as="order" t-key="order.id">
                            <!-- ✅ CHỈ HIỆN ĐƠN CÓ MÓN -->
                            <t t-if="hasVisibleLines(order.id)">
                                <!-- Order Card -->
                                <div t-att-class="'flex flex-col rounded-xl bg-white shadow-md ' + getOrderBorderClass(order)">
                                    <div class="flex grow flex-col">
                                        <div class="flex grow flex-col gap-3 p-4">
                                            <!-- Order Header -->
                                            <div class="flex items-center justify-between gap-3">
                                                <p class="text-3xl font-bold leading-tight tracking-tight text-gray-800">
                                                    <!-- Prefix: Preset Name -->
                                                    <t t-if="order.preset_id">
                                                        <t t-esc="order.preset_id[1]"/>
                                                    </t>
                                                    - 
                                                    <!-- Suffix: Table Name or Order Ref -->
                                                    <t t-if="order.table_id">
                                                        <t t-esc="order.table_id[1]"/>
                                                    </t>
                                                    <t t-else="">
                                                        <t t-esc="order.pos_reference or order.name"/>
                                                    </t>
                                                </p>
                                                <p t-att-class="'text-lg font-medium leading-normal ' + getTimeClass(order)">
                                                    <t t-esc="getRelativeTime(order.date_order)"/>
                                                </p>
                                            </div>

                                            <!-- Divider -->
                                            <div class="-mx-4 border-t border-gray-200"></div>

                                            <!-- Order Items -->
                                            <div class="flex flex-col divide-y divide-gray-200">
                                                <t t-foreach="getOrderLines(order.id)" t-as="line" t-key="line.id">
                                                    <div t-att-class="'relative py-3 ' + (line.trcf_order_status !== 'draft' ? 'line-completed' : '')">
                                                        <div class="flex flex-wrap items-center justify-between gap-x-4 gap-y-2">
                                                            <div class="item-content flex-1">
                                                                <div class="flex items-center gap-3">
                                                                    <span class="inline-flex h-10 w-10 shrink-0 items-center justify-center rounded-lg bg-orange-600 text-2xl font-black text-white shadow-sm ring-2 ring-orange-200">
                                                                        <t t-esc="line.qty"/>
                                                                    </span>
                                                                    <p class="text-base font-bold leading-tight text-gray-800 flex-1">
                                                                        <t t-esc="line.full_product_name || line.product_id[1]"/>
                                                                    </p>
                                                                </div>
                                                                <t t-if="line.note">
                                                                    <p class="item-note text-base text-orange-600 font-medium pt-3">
                                                                        <t t-esc="getNote(line.note)"/>
                                                                    </p>
                                                                </t>
                                                            </div>
                                                            <button 
                                                                t-on-click="() => this.markOrderLineReady(line.id)"
                                                                t-att-disabled="isOrderLineLoading(line.id) or line.trcf_order_status !== 'draft'"
                                                                t-att-class="'rounded-lg px-4 py-2 text-sm font-bold transition-colors disabled:cursor-not-allowed ' + (line.trcf_order_status !== 'draft' ? 'bg-green-600 text-white' : 'bg-gray-200 text-gray-700 hover:bg-gray-300')">
                                                                <t t-if="isOrderLineLoading(line.id)">
                                                                    ⏳ Đang xử lý...
                                                                </t>
                                                                <t t-elif="line.trcf_order_status !== 'draft'">
                                                                    ✓ Đã xong
                                                                </t>
                                                                <t t-else="">
                                                                    Xong
                                                                </t>
                                                            </button>
                                                        </div>

                                                        <t t-if="state.showRecipe and line.public_description">
                                                            <div
                                                                class="recipe-content mt-2 rounded-lg bg-blue-50 px-3 py-2 text-base text-blue-800">
                                                                <t t-out="renderHtml(line.public_description)"/>
                                                            </div>
                                                        </t>
                                                    </div>
                                                </t>
                                            </div>
                                        </div>
                                    </div>

                                    <!-- Complete Button -->
                                    <!--div class="border-t border-gray-200 p-3">
                                        <button 
                                            t-on-click="() => this.markAsDone(order.id)"
                                            t-att-disabled="isOrderLoading(order.id)"
                                            class="w-full rounded-lg bg-green-600 py-4 text-lg font-bold text-white transition-colors hover:bg-green-700 disabled:opacity-50 disabled:cursor-not-allowed">
                                            <t t-if="isOrderLoading(order.id)">
                                                ⏳ Đang xử lý...
                                            </t>
                                            <t t-else="">
                                                ✓ Hoàn thành
                                             </t>
                                        </button>
                                    </div-->
                                </div>
                            </t>
                        </t>

                        <!-- Empty State -->
                        <t t-if="state.draft_count === 0">
                            <div class="col-span-full flex flex-col items-center justify-center py-20">
                                <span class="text-6xl">📋</span>
                                <h3 class="mt-4 text-xl font-semibold text-gray-500">Không có đơn hàng mới</h3>
                                <p class="mt-2 text-gray-400">Tất cả đơn hàng đã được xử lý</p>
                            </div>
                        </t>
                    </div>
                </main>

                <!-- Summary Footer -->
                <footer t-if="state.showSummary and aggregatedSummary.length > 0" 
                        class="sticky bottom-0 z-20 w-full border-t border-gray-200 bg-white/95 px-4 pt-4 pb-[calc(1rem+env(safe-area-inset-bottom))] shadow-2xl backdrop-blur-sm">
                    <div class="mb-3 flex items-center justify-between">
                        <h2 class="text-lg font-bold text-gray-800 flex items-center gap-2">
                            <span class="material-symbols-outlined text-orange-600">analytics</span>
                            Tổng hợp món đang chờ
                        </h2>
                        <span class="text-sm font-medium text-gray-500">
                            <t t-esc="aggregatedSummary.length"/> món khác nhau
                        </span>
                    </div>
                    <div class="max-h-[25vh] overflow-y-auto">
                        <div class="flex flex-wrap gap-2">
                            <t t-foreach="aggregatedSummary" t-as="item" t-key="item.full_product_name">
                                <div class="flex items-center gap-2 rounded-xl bg-orange-50 border border-orange-100 px-4 py-2 shadow-sm transition-all hover:shadow-md">
                                    <span class="text-xl font-black text-orange-600">
                                        <t t-esc="item.qty"/>
                                    </span>
                                    <span class="text-base font-bold text-gray-800">
                                        <t t-esc="item.full_product_name"/>
                                    </span>
                                    <!-- Nút Xong hàng loạt -->
                                    <button t-on-click="() => this.markBatchAsReady(item.full_product_name)"
                                            class="ml-2 rounded-lg bg-green-600 px-3 py-1 text-sm font-bold text-white shadow-sm transition-colors hover:bg-green-700 active:scale-95">
                                        XONG
                                    </button>
                                </div>
                            </t>
                        </div>
                    </div>
                </footer>
            </div>
        </div>
    </t>
</templates>